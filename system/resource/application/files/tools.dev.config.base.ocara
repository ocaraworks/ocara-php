<?php
/**
 * 基本配置
 */
return array(
    'DEFAULT_CONTROLLER' => 'generate',
    'DEFAULT_ACTION' => 'index',

    /**
     * 访问开发者中心时校验IP和域名配置
     */
    'LIMIT' => array(
        'server_ips' => array('127.0.0.1'), //服务器IP白名单
        'server_domains' => array('localhost'), //服务器域名白名单
    ),
);